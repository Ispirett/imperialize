const state = {
    fileSource: 'https://www.soundhelix.com/examples/mp3/SoundHelix-Song-1.mp3'
}
const getters = {
    allUsers: (state) => state.users
}
const actions = {}
const mutations = {};


export  default {
    state,
    getters,
    actions,
    mutations
}